# Khi import x = {name: "tung"} vào file1 và file2, và biến đổi x.name trong file1, thì x trong file2 có bị ảnh hưởng không?
- CÓ
- vì đối tượng trong các file đều trỏ đến cùng 1 đối tượng gốc


# Object.is()