# useCallback()

# chứ năng:

- cache 1 đối tượng function
- tái sử dụng

# các kịch bản phổ biến

- dùng function làm dependency cho 1 hook
- truyền function vào memo component
- update state (dùng với setState(()=> {}))
- bọc các return function trong custom Hook
- "dùng với render list"
